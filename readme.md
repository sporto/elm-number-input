# Elm Number Input

## WIP An input that restricts inputs to numbers

- Prevent input of non numbers or dot
- Allow dot
- Prevent adding dot twice
- Allow inserting anywhere (Cursors shouldn't jump)
- Allow backspace
